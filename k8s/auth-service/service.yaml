apiVersion: v1
kind: Service
metadata:
  name: auth-service
  namespace: esn-events

spec:
  type: ClusterIP

  selector:
    app: auth-service

  ports:
    - name: http
      port: 8085
      targetPort: 8085