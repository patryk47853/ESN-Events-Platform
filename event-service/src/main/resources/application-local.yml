spring:
  config:
    activate:
      on-profile: local

  datasource:
    url: jdbc:postgresql://localhost:5433/esn_events
    username: postgres
    password: postgres

  jpa:
    hibernate:
      ddl-auto: create-drop

  sql:
    init:
      mode: always