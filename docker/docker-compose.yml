version: '3.8'

services:
  postgres:
    image: postgres:15
    container_name: esn-postgres
    environment:
      POSTGRES_DB: esn_events
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "5433:5432"